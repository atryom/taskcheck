<%= form_for @task, as: :task, url:group_task_path(params[:group_id], @task), html: { class:"bs-example form-horizontal"} do |f|%>

    <% @frm = f%>
    <%= render partial:"form", locals: {title: @title, btn_lbl:@btn_lbl, frm:@frm} %>

<% end %>

<%= link_to 'К списку задач', group_tasks_path(params[:group_id]) %>
